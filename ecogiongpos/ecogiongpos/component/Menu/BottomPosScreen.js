import React, { Component } from 'react'
import { View, Text, StyleSheet, TouchableOpacity, ActivityIndicator, ScrollView, Dimensions} from 'react-native'
import Icon from 'react-native-vector-icons/Ionicons'
import Feather from 'react-native-vector-icons/Feather' 

export default class BottomPosScreen extends Component{
    render(){
        return(
            <View style={style.container}><Text>fffff</Text></View>
        );
    }
}
const style = StyleSheet.create({
    container:{
       
    },
})